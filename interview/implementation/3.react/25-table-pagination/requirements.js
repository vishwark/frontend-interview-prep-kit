/**
 * Table with Pagination Requirements
 * 
 * Implement a data table with pagination functionality:
 * 
 * 1. Create a table component with the following features:
 *    - Display data in rows and columns
 *    - Support for custom cell rendering
 *    - Sortable columns (ascending/descending)
 *    - Column resizing (optional)
 *    - Row selection (single or multiple)
 *    - Fixed header with scrollable body
 * 
 * 2. Implement pagination with:
 *    - Next/Previous page navigation
 *    - Page number selection
 *    - Items per page selector
 *    - Display of total items and current range
 * 
 * 3. Additional features:
 *    - Search/filter functionality
 *    - Column visibility toggle
 *    - Row expansion for additional details
 *    - Export data functionality (CSV, Excel)
 * 
 * Technical requirements:
 * - Create a reusable and configurable component
 * - Support for client-side or server-side pagination
 * - Handle loading, error, and empty states
 * - Implement proper accessibility features
 * - Create a responsive design with mobile considerations
 * - Optimize performance for large datasets
 * - Support for keyboard navigation
 */
